This is the offline branch predictor ECE511 project by:
Neil Singh, Jay Patel, Tim Wang

To build the project, run the build script by running this command;

./build.sh

To run the simulation run:
./rungem5.sh <benchmark>

The runnable values of <benchmarks> are:
    -soplex: runs the soplex benchmark
    -libquantum (DOES NOT WORK)
    -hello: runs a hello world benchmark
